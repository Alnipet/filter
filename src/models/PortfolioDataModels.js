export class ItemCategory {
  constructor(itemCategory) {
    this.itemCategory = itemCategory;
  }
}

export class ActiveCategory {
  constructor(activeCategory) {
    this.activeCategory = activeCategory;
  }
}

export class OnSelectFilter {
  constructor(onSelectFilter) {
    this.onSelectFilter = onSelectFilter;
  }
}